<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tom.cloud.mapper.UserMapper">
    <!--/**
     * 查询全部的user
     * @return
     */
    List<User> findAllUser();-->
    <select id="findAllUser" resultType="com.tom.cloud.entities.User">
        select * from user
    </select>
</mapper>